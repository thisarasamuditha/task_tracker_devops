# Development configuration for local testing. 
spring.application.name=demo

# H2 In-Memory Database (No installation required - perfect for development)
spring.datasource.url=jdbc:h2:mem:taskdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (Access at http://localhost:8088/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Server configuration
server.port=8088

# CORS configuration for local frontend development
# (If needed, can be configured in SecurityConfig.java)

## ALTERNATIVE: MySQL Configuration (if you have MySQL installed)
## Uncomment these and comment out H2 settings above
#spring.datasource.url=jdbc:mysql://localhost:3306/taskdb?createDatabaseIfNotExist=true
#spring.datasource.username=YOUR_MYSQL_USERNAME
#spring.datasource.password=YOUR_MYSQL_PASSWORD
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
